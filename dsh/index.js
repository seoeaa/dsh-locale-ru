// dsh-locale-ru server half: the work happens in the browser (./client.js).
// This plugin exists so the bundle mounts and the client half is discovered.
export const name = 'dsh-locale-ru'
export function apply() {}
